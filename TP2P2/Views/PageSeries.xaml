<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TP2P2.Views.PageSeries"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TP2P2.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid>
        <controls:DataGrid x:Name="DataGridSeries"
                       ItemsSource="{Binding Series}" 
                       AutoGenerateColumns="False"
                       IsReadOnly="True"
                       GridLinesVisibility="All"
                       BorderThickness="1" BorderBrush="Gray">

            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="ID" Binding="{Binding Serieid}" Width="50"/>

                <controls:DataGridTextColumn Header="Titre" Binding="{Binding Titre}" Width="*"/>

                <controls:DataGridTextColumn Header="Chaîne" Binding="{Binding Network}" Width="100"/>

                <controls:DataGridTextColumn Header="Année" Binding="{Binding Anneecreation}" Width="80"/>

                <controls:DataGridTextColumn Header="Saisons" Binding="{Binding Nbsaisons}" Width="80"/>
                <controls:DataGridTextColumn Header="Episodes" Binding="{Binding Nbepisodes}" Width="80"/>

                <controls:DataGridTextColumn Header="Résumé" Binding="{Binding Resume}" Width="300"/>
            </controls:DataGrid.Columns>

        </controls:DataGrid>
    </Grid>
</Page>
